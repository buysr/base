apiVersion: v2
name: parent-chart
description: Parent chart that tries to read files from subchart (WILL FAIL)
version: 1.0.0
dependencies:
  - name: child-chart
    version: "1.0.0"
    repository: "file://charts/child-chart"
